// import type { SuccessResponse } from "~/utils/model/response";

// export async function customFetch<T>(url: RequestInfo, options?: ): Promise<SuccessResponse<T>> {
//     const response = await $fetch(url, options);
// }